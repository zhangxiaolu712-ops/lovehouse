# 2026-08-23 工程错题集 V1

## 范围

- 新建 `docs/LoveHouse_工程错题集_V1.md`，收录 16 条经代码、Git、测试、migration 或带时间工程记录复核的故障模式。
- 新建 `docs/LoveHouse_工程错题集_V1_待人工确认.md`，隔离 6 条缺少失败现场或真实客户端回执的候选，并额外记录 1 条生产状态未复核的 Worker 候选。
- 长对话和压缩摘要只用于发现候选，没有作为正式条目的事实证据。

## 证据范围

- Git commits/diffs 与当前 main 源码。
- `bridge/*` 自动测试、Supabase migrations/data migration。
- `docs/changes/*` 的带日期施工与 smoke 记录。
- `gate-a-backups/*` 中已保存的只读/部署输出。

## 发现的文档漂移

- `docs/ARCHITECTURE_CURRENT.md:221-223` 仍写 14 tools；当前 main 为 7 tools。
- `docs/02_当前架构.md:171-197` 仍写 Memory V2 未接 MCP/Bridge；当前 main 已注册 V2 facade/transport。
- `CLAUDE.md:98` 写所有表 RLS 关闭；当前 V2 migration 对五张表启用并 FORCE RLS。
- 多份 `docs/changes/*` 的“未部署”是当时状态，不能作为当前生产结论。

## 边界

- 本轮只新增文档。
- 未修改业务代码、数据库、OAuth、Memory、PM2、Nginx、Worker 或生产环境。
- 未提交、未 push、未创建 PR、未部署。
