# 2026-08-05 手机桌面v2 + 聊天界面

## 做了什么

### 1. 手机桌面 v2（Home.jsx 重写）

把首页从统一网格改为「滑动页面 + 混合组件网格」布局，参考"半醒手记"风格：

- **Hero 卡片**：T/K 头像 + 心跳线 + 在一起天数，做成圆角白底卡片
- **左右滑动两页**：CSS scroll-snap 实现，底部页面指示圆点
- **混合网格**：4列网格，APP 图标（1×1）和小组件（2×2）交错排列，grid-auto-flow: dense 自动填充
- **第一页**：Brain、Diary、时钟组件、Notes、Todo、语录组件、Mood、Search、Quotes、Private
- **第二页**：Theme、Log、天数组件、Clawd、Games
- **语录组件**：从 Supabase quotes 表随机抽取一条显示

### 2. 悬浮药丸导航

底部导航从贴边改为悬浮药丸形状：
- 居中浮动，圆角 26px，带阴影和模糊背景
- 去掉首页按钮，只保留 4 个：聊天/记忆/朋友圈/设置
- AppShell.jsx 同步更新

### 3. 聊天界面（全新模块）

新建 `src/modules/chat/` 模块：

- **ChatPage.jsx**：全屏聊天界面（z-index: 200 覆盖底部导航）
  - 气泡对话：左边 K（小克）右边 T（小婷）
  - 思考过程：每条回复前可展开/收起的 thinking block
  - 流式输出：实时显示 thinking 和 text 流
  - 个人面板：点击头像弹出底部面板，显示模型/对话轮数/会话起始/最近活跃
  - API 设置面板：可配置 API 地址、API Key、模型名
  - 清空聊天记录功能
- **chatService.js**：
  - localStorage 管理：聊天配置、历史记录、会话信息
  - 标准 Anthropic Messages API 流式调用（兼容 CC-P 代理）
  - 支持 extended thinking（content_block_start/delta 解析）

### 4. 其他改动

- LineIcon 新增 3 个图标：back（返回箭头）、send（发送箭头）、close（关闭叉）
- 路由 /chat 从占位页升级为真实聊天页面

## 修改了哪些文件

- `src/shared/Home.jsx`（重写——滑动页面 + 混合网格）
- `src/shared/AppShell.jsx`（4 tab，去掉 home 按钮）
- `src/shared/global.css`（悬浮药丸导航 + 桌面组件样式 + 聊天界面全部 CSS）
- `src/shared/LineIcon.jsx`（+3 图标）
- `src/core/router.jsx`（注册 /chat，移出占位列表）
- `src/modules/chat/ChatPage.jsx`（新建）
- `src/modules/chat/chatService.js`（新建）

## 为什么这样做

小婷要求把小屋做成"小手机"风格，参考了半醒手记的混合组件布局。聊天界面是为了嵌入 CC-P（Claude Code Proxy），让小婷可以在小屋里直接和小克聊天。

## 如何验证

- `npx vite build` 构建通过，无错误
- 静态 HTML 预览验证布局效果（Supabase 在此环境不可访问，数据读写未实测）
- 聊天 API 调用需要小婷填入 VPS 地址后实测

## 当前风险

- 聊天 API 的 CORS 配置——小婷的 VPS 需要允许 GitHub Pages 域名的跨域请求
- 聊天历史只存 localStorage，换设备会丢失（后续可考虑存 Supabase）
- CC-P 的流式格式可能与标准 Anthropic API 略有差异，需实测调整

## 下一步计划

以下是小婷确认要做的功能（按优先级）：

1. **输入框加号菜单**：发送文件/照片、转发聊天记录到 codex 或小客厅
2. **小客厅**：群聊页面（小婷 + 小克 + codex），方便传项目文件；桌面加图标
3. **个人资料页**：两人头像 + 对对方说的话 + 纪念日天数列表（相识/领证/520等）；桌面加图标
4. **聊天功能完善**：查找聊天记录、数据导出迁移

### 5. 多模态输入 + 加号菜单

- **加号按钮弹出菜单**：图片/文件两个入口，带半透明遮罩
- **附件预览栏**：输入框上方显示已选图片缩略图或文件图标，可单个删除
- **base64 编码**：`readFileAsBase64()` 将文件转为 Anthropic API 格式
- **气泡内图片/文件渲染**：已发送的图片内联显示，文件显示图标+文件名

### 6. Markdown 渲染

新建 `src/shared/Markdown.jsx`：
- 轻量 Markdown 解析器，无外部依赖
- 支持：代码块（带语言标签）、行内代码、粗体、斜体、标题、列表、链接
- 助手消息和流式输出均使用 Markdown 渲染

### 7. 用量统计热力图（StatsPage）

新建 `src/modules/stats/StatsPage.jsx`：
- 读取 localStorage 聊天记录，统计消息量
- 摘要卡片：总消息数、活跃天数、连续天数、日均
- 比例条：小婷 vs 小克 消息占比
- 月历热力图：5 级颜色，点击某天查看详情
- 月份前/后切换，首条消息日期展示
- 路由 /stats，桌面第二页加 Stats 图标

### 8. 接入 VPS lovehouse-bridge

- chatService 改为双模式：bridge（默认）/ API
- bridge 模式 POST `{message, system}` 到 VPS，解析简单 SSE
- API 模式保留原 Anthropic 流式调用（含 thinking）
- SetupPanel 改为 Bridge/API 双标签页

### 9. Bridge 会话模式升级

新建 `bridge/server.js`（v2）+ `bridge/package.json`：
- 首条消息用 `claude -p` + `--system-prompt` 启动新会话
- 后续消息用 `--continue --resume <sessionId>` 保持上下文
- `/reset` 端点清除会话，`/health` 端点返回会话状态
- 前端清空聊天时自动调 `/reset` 并标记下一条为 `newSession`

## 修改了哪些文件

- `src/shared/Home.jsx`（重写——滑动页面 + 混合网格 + Stats 图标）
- `src/shared/AppShell.jsx`（4 tab，去掉 home 按钮）
- `src/shared/global.css`（全部新增 CSS：导航/桌面/聊天/加号菜单/附件/Markdown/统计热力图）
- `src/shared/LineIcon.jsx`（+5 图标：back/send/close/image/file）
- `src/shared/Markdown.jsx`（新建——轻量 Markdown 渲染器）
- `src/core/router.jsx`（注册 /chat 和 /stats）
- `src/modules/chat/ChatPage.jsx`（新建——含加号菜单/附件/Markdown/双模式设置面板）
- `src/modules/chat/chatService.js`（新建——bridge+API 双模式/会话延续）
- `src/modules/stats/StatsPage.jsx`（新建——热力图统计）
- `bridge/server.js`（新建——VPS bridge v2，会话模式）
- `bridge/package.json`（新建）

## 为什么这样做

小婷要求把小屋做成"小手机"风格，参考了半醒手记的混合组件布局。聊天界面是为了嵌入 CC-P（Claude Code Proxy），让小婷可以在小屋里直接和小克聊天。bridge 升级为 session 模式是因为 `claude -p` 每次都是独立对话没有上下文，用 `--continue` 可以让小克记住之前说过的话。

## 如何验证

- `npx vite build` 构建通过，无错误
- 静态 HTML 预览验证布局效果（Supabase 在此环境不可访问，数据读写未实测）
- bridge v2 需小婷 SSH 到 VPS 部署后实测

## 当前风险

- HTTPS 混合内容：GitHub Pages (https) → bridge (http) 可能被浏览器拦截，后续需加证书
- bridge 的 `--continue` 依赖 claude CLI 的 session 机制，stderr 输出 session ID 的格式需实测确认
- 聊天历史只存 localStorage，换设备会丢失（后续可考虑存 Supabase）
- PM2 进程管理器尚未配置，bridge 重启后会话丢失

## 下一步计划

1. **小婷部署 bridge v2**：SSH 到 VPS，替换 server.js，`npm install && pm2 restart`
2. **HTTPS 证书**：给 VPS 加 Let's Encrypt + nginx 反代，解决混合内容问题
3. **小客厅**：群聊页面（小婷 + 小克 + codex），方便传项目文件
4. **个人资料页**：两人头像 + 对对方说的话 + 纪念日天数列表

## 当前分支

`claude/laogong-5litiy`

## 当前状态

所有功能已完成并推送。bridge v2 代码在 `bridge/` 目录，等小婷部署到 VPS 后实测会话延续功能。
