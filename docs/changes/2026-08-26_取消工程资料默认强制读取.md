# 取消工程资料默认强制读取

- 日期：2026-08-26
- 范围：#63 的共同开发入口与工程资料读取规则
- 状态：已修改，待 #63 验收合并

## 做了什么

- 保留 `PROJECT_RULES.md` 作为施工前唯一默认必读规则入口。
- 将 `CLAUDE.md`、`docs/00_工程边界与资料索引.md`、`docs/changes/` 和 Engineering Workspace 历史工程资料改为按任务需要查询。
- 只有任务涉及相关历史、工程资料迁移、来源追溯，或代码与现状说明冲突时，才读取直接相关部分。
- 将 `CLAUDE.md` 的“AI 自动读取指令”改为“AI 按需读取指引”，保留 Memory V2 与工程/生活资料分流边界。

## 为什么这样做

#62 的工程资料改动已由 #63 包含，不再维持两条重复施工主线。同时避免每次新对话或接班时无条件加载 `CLAUDE.md`、资料索引和历史工程资料，让读取范围由当前任务决定。

## 修改文件

- `AGENTS.md`
- `PROJECT_RULES.md`
- `CLAUDE.md`
- 本变更记录

## 数据库、环境变量与部署

- 数据库：无变化。
- 环境变量：无变化。
- 部署：无变化。

## 实际验证

- 检查默认入口文字，确认不再要求每次施工或每次新对话强制读取 `CLAUDE.md`、`docs/00_工程边界与资料索引.md`、`docs/changes/` 或 Engineering Workspace 历史资料。
- 确认 `PROJECT_RULES.md` 仍为唯一默认必读的共同开发规则。

## 已知风险或未完成事项

- #63 尚待验收、合并。
- #62 应在 #63 验收合并后关闭；本次不单独施工或合并 #62。

## 下一步计划

1. 在 #63 完成规则修正的 diff 验收。
2. 合并 #63。
3. 合并完成后关闭 #62，避免重复主线。
