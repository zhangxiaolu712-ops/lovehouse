# 工程资料收口与规划边界

- 日期：2026-08-26
- 执行者：GPT-5.6 Sol
- 状态：部分完成（边界与来源地图已建立；Supabase 已完成首批 6 个 Engineering subject 导入与读回验收；B612-only source 转交、旧目录清理、GitHub 合并/部署尚未执行）

## 做了什么

- 新建长期入口 `docs/00_工程边界与资料索引.md`，明确：
  - Engineering Workspace 保存详细工程事实、revision、source evidence 与错误经验。
  - GitHub / VPS 文档只保留当前规则、当前架构、当前规划、索引和与代码对应的 change record。
  - 旧 checkout / worktree / release 仅作为历史证据或迁移来源。
- 在 `PROJECT_RULES.md` 增加“工程资料归属与清理边界”，把上述规则提升为共同开发约定。
- 以 `origin/main@c894913e1548cc89b9ccf6868d400b51e62fa149` 的文档树为基线，对已知 VPS 旧目录做只读差异扫描。
- 确认 `lovehouse-deploy-df007fa5` 中三份工程错题集文件是当前主干没有的 unique orphan，迁移验收前禁止删除。
- 初步收敛出需要迁移判断的少量独有文档；大量历史 release 中的 `docs/02_当前架构.md` / `docs/06_待开发列表.md` 只是不同时间切片，不应作为独立 current 文档保留。
- 新建过渡清单 `docs/engineering-import-2026-08-26.md`，把第一批 Engineering `subject_key`、App 文件库来源、VPS current/historical/unique_orphan 分类、worktree/release 清理门禁和实际导入顺序固定下来。
- 重新实测当前 PM2 / listener / Nginx：Bridge=`c894913...:3000`、Codex=`cd011513...:3002`、Claude=`6c8c475...:3003`，静态根仍为 `/root/lovehouse-dist`；这些作为当前 revision 的候选事实，不沿用旧 PID 快照。
- 发现 `lovehouse-deploy-3f18558d` 不只是旧文档：还存在未跟踪 `gpt-memory`、`unified-chat`、`memoryTimeline`、`project` 等代码，以及路由/首页未提交差异；因此该 worktree 不能按“重复目录”直接删除。
- 核对未合并 branch：`feat/b612-project-checklist-v1` 仍有 `400dd5c` / `f14e134` 两个 current main 未包含的 commit；`agent/codex-voice-frontend-20260824` 的 `eda2b083` 也仍是 unique commit。清理 worktree 时 branch ref 与代码去留必须单独处理。
- 用户从独立 Supabase-only 窗口回传首批正式导入结果：6 个 subject、7 个 revision、7 条 source link 已通过 subject_key/current_revision/history/source/quote_text/open/recall 验收；`smoke.backend-write.20260825-2120` 继续 archived。该批没有调用 B612。
- 明确区分 `lessons.engineering-mistakes.migration`（迁移过程/状态）与待导入的 `lessons.engineering-mistakes` 正文；三份错题 Markdown 原文尚未跨到 Supabase 路线，不能因“migration subject 已有”就清掉 orphan。

## 为什么这样做

此前 LoveHouse 的工程事实散在官方 App 文件库、正式仓库、旧 checkout、DevSpace worktree、历史 release 和临时 handoff 中。继续依赖“翻目录找旧纸”会让当前事实与历史状态混淆，也增加误删独有成果的风险。

本轮先建立清晰的资料归属和删除门禁，再执行 Engineering 批量迁移与旧目录清理，避免先删后补。

## 修改文件

- `docs/00_工程边界与资料索引.md`：新增长期工程资料入口、subject_key 规划、来源优先级、已知位置、清理门禁。
- `PROJECT_RULES.md`：新增工程资料归属与清理规则。
- `docs/engineering-import-2026-08-26.md`：本轮过渡导入清单；导入完成后不承担长期入口职责。
- `docs/changes/2026-08-26_工程资料收口与规划边界.md`：本记录。

## 数据库、环境变量与部署

- 数据库：本 B612 窗口未修改数据库；独立 Supabase-only 窗口已完成首批 6 个 Engineering subject / 7 revision / 7 source link 正式写入与读回验收。
- 环境变量：无变化。
- PM2 / Nginx / Cloudflare / 生产 release：无变化。
- GitHub：本轮文件目前只存在于隔离 VPS worktree，尚未 commit / push / PR / merge。

## 验证结果

已执行：

- 读取 `PROJECT_RULES.md`、`CLAUDE.md`、`docs/02_当前架构.md`、`docs/06_待开发列表.md` 与近期 Engineering / Bridge change records。
- `git worktree list --porcelain` 核对当前旧 checkout / worktree 分布。
- 以当前 `c894913...` 文档树为基线，对旧 checkout、Bridge 目录、DevSpace worktree、历史 release 做只读文件差异比较。
- 当前 PM2 / `ss` / `nginx -T` 只读核验生产 release、端口与静态 root。
- 对各 worktree 执行 `origin/main...HEAD` 关系、dirty 状态与 unique commit 核对。
- 将 `lovehouse-deploy-3f18558d` 未跟踪代码逐路径与 current main 比较，确认其中多项代码在 current main 中不存在，不能误删。
- 确认以下 unique orphan 仍为未跟踪文件：
  - `/root/.devspace/worktrees/lovehouse-deploy-df007fa5/docs/LoveHouse_工程错题集_V1.md`
  - `/root/.devspace/worktrees/lovehouse-deploy-df007fa5/docs/LoveHouse_工程错题集_V1_待人工确认.md`
  - `/root/.devspace/worktrees/lovehouse-deploy-df007fa5/docs/changes/2026-08-23_工程错题集V1.md`

未执行：

- B612-only 原始 source 的第二批 Engineering import/read-back（首批 Supabase-only 导入已完成）。
- 任何旧文件 / worktree / release 删除。
- Git commit / push / PR / merge。
- 生产部署。

## 已知风险或未完成事项

- Engineering 写入通道已在独立 Supabase-only 窗口验证可用；当前真正阻塞是 B612/VPS-only 原始 source 无法在同一窗口直接读取。需通过可验证的 source handoff 跨过插件互斥，而不是让 Supabase 窗口根据摘要重写。
- `lovehouse-deploy-3f18558d` 的未跟踪代码属于代码资产，不是 Engineering memory 能替代的内容；清理前必须先保全或明确判定废弃。
- 旧目录中 `docs/02_当前架构.md` / `docs/06_待开发列表.md` 内容不同并不代表都要保留；需按 subject revision 拆解后再清理。
- `/root/lovehouse-deploy` 当前不是 current main，不能直接作为主干继续施工。
- 当前本地 `main` ref 与 `origin/main` 曾观察到漂移；正式清理时要统一 ref，避免从旧 main 开新工单。

## 下一步计划

1. 将 B612/VPS-only 原始 source 做成可验证 handoff，交给 Supabase-only 窗口；App 文件库主资料继续直接走 Files。
2. 在 Supabase-only 窗口继续按 subject_key → revision → source → read-back 导入剩余主题，尤其先导入工程错题正文与 VPS current facts。
3. 确认 unique orphan 已迁移后，形成“保留 / 删除 / 归档”清单。
4. 分批清理废弃 worktree、重复 snapshot 与临时 handoff；保留当前生产和必要 rollback。
5. 根据清理后的真实状态更新 `docs/02_当前架构.md` 与 `docs/06_待开发列表.md`，再 commit / push / PR 到 GitHub。
