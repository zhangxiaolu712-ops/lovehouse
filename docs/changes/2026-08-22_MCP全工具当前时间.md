# MCP 全工具统一返回当前时间

- 日期：2026-08-22
- 执行者：Codex
- 状态：本地完成并待审；未 push、未 merge、未部署
- 基线：`97b62cbd2f8c6f3f165b23a3bdbce009c4834b6e`
- 分支：`agent/mcp-called-at-20260822`

## 做了什么

- 在统一 MCP `tools/call` 响应层生成一次服务端当前时间，并向七个正式工具的原 JSON 结果顶层增加 `called_at`。
- `called_at` 使用带毫秒与明确 `+08:00` 偏移的 ISO 8601 格式；工具自身的业务字段保持不变。
- 工具执行层抛出的错误继续走既有 JSON-RPC error，并在 `error.data.called_at` 中携带同一次调用时间。
- GPT OAuth、Claude OAuth 与旧 GPT SSE transport 共用同一实现；七个工具内部没有重复时间逻辑。

## 修改文件

- `bridge/mcp/transports.js`：统一生成并注入 `called_at`，统一补充执行层错误时间元信息。
- `bridge/mcp/transports.test.js`：覆盖七个工具、GPT/Claude、字段保留、ISO 可解析、`+08:00` 与错误响应。
- `bridge/oauth.test.js`：更新真实 OAuth → MCP tool call 集成断言，验证固定 actor 与 `called_at` 同时存在。
- `docs/changes/2026-08-22_MCP全工具当前时间.md`：本记录。

## 数据库、环境变量与部署

- Memory V2 schema、RPC、RLS、Memory/LivingRoom 自身时间字段均未修改。
- 没有新增表、环境变量、安全层、worker 或服务。
- 没有修改生产环境、部署 Bridge 或重启 PM2。

## 验证结果

- MCP/OAuth 定向测试：24/24 PASS。
- Bridge 全量测试：187/187 PASS。
- 七工具 × GPT/Claude：14 个统一响应断言全部 PASS。
- `called_at` 可被 `Date.parse` 解析并精确以 `+08:00` 结尾。
- 原业务字段保留；执行层错误的 `error.data.called_at` 通过。
- `git diff --check`：PASS。

## 已知风险或未完成事项

- 本提交尚未 push、PR、merge 或部署，生产 MCP 暂无 `called_at`。
- 统一层要求正式工具继续返回 JSON object；当前七个工具均满足这一既有契约。

## 下一步计划

1. 停止并等待人工复审；部署必须另行授权。
