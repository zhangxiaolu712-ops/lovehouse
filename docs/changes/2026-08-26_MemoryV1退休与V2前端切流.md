# Memory V1 退休与 V2 前端切流

- 日期：2026-08-26
- 范围：Memory V1 frontend retirement、GPT Memory V2、Project Checklist server persistence
- 状态：实现与生产 schema 已完成；等待 production release smoke 后执行 V1 freeze 与最终收口。

## 做了什么

- 从 `archive/3f18558d-unique-code-20260826` 复核并按当前 Owner Client API/Auth 契约接回 GPT Memory timeline，正式路由为 `/memory/gpt`。
- 新增 Owner-auth `/api/v1/memory/:actor/*`：timeline、remember、revise、history、source expansion、archive。actor 只允许 `gpt`/`claude`；timeline 只返回 active current revision；revise 复用 append-only `memory_v2_revise`。
- `/brain`、`/memory` 与全屋搜索的记忆来源全部切到 Claude Memory V2；移除 awaken/faded 与 legacy weight 行为，删除映射为 V2 soft archive，特殊标记映射为新 revision metadata。
- Project Checklist 保留代码内基线，新增 Engineering domain 的 `engineering_project_checklist_items/state` 与 Owner API。状态、note、completedAt、自定义项以服务端为 canonical；旧 `b612.project-checklist.v1` 只在服务端尚未标记迁移时提交一次，事务成功后删除本地旧值。
- 新增独立 freeze migration：仅在新链生产 read-back PASS 后撤销 `brain`、`memories`、`memory_entries` 的 anon/authenticated 权限并标记 archived/frozen；不 DROP 表、不删除 release/rollback。

## 数据库与权限

- 已应用 `memory_v2_frontend_and_engineering_checklist`：2 个 Memory V2 RPC、2 张 Engineering Checklist 表、4 个 Checklist RPC。
- 新表强制 RLS；anon/authenticated 无表权限，只有 Bridge service role 可访问；浏览器必须通过 Owner bearer API。
- `20260826170000_freeze_memory_v1_tables.sql` 必须等 production read-back 后单独应用。

## 验证

- `npm run lint`：PASS（仅既有 warnings）。
- `npm run build`：PASS。
- Frontend service tests：PASS。
- Bridge `npm test`：217/217 PASS（回环监听需在 sandbox 外执行）。
- production schema read-back：GPT 85 active current、Claude 456 active current；timeline RPC 分别读到 GPT 85、Claude page limit 100。
- rollback transaction smoke：remember → revise → current revision 指向新 revision、2 revision 保留、source 随 revision 保留；Checklist local migration 第二次返回 `already_migrated`，server save/read-back PASS。

## 风险与下一步

- 生产 Bridge/静态 release、HTTPS 页面 smoke、旧表 freeze、Engineering continuity revision 尚待本轮后续完成。
- 旧表本轮不 DROP；最终可删除判断必须结合部署后 reader/writer 扫描与观察结果。
