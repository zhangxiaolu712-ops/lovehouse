# 玫瑰背景、轻斜字体与搭建日志续写

- 日期：2026-07-30
- 执行者：Codex
- 状态：已完成本地实现与验证，等待 Cloudflare 预览确认

## 做了什么

- 完整读取用户提供的《前端界面与代码整理规范》，按“背景、字体、图标和布局互相独立”的原则接入新视觉。
- 为星球玫瑰主题新增独立背景配置，将用户确认可商用的粉色纸纹玫瑰素材作为单独资源管理。
- 将同一背景接入开屏、账号登录、PIN 门锁和进屋后的应用外壳。
- 降低卡片、页面纸张、导航栏、输入框和登录框的不透明度与模糊强度，使背景能够透出。
- 只让标题、纪念天数和英文装饰轻微倾斜，正文保持端正。
- 在站内“搭建日志”补写 2026-07-26 与 2026-07-27 的 Codex 工作摘要，并将旧时间线圆点的 Emoji 统一为线性图标。

## 为什么这样做

- 用户希望背景可以单独替换，而不是再次重做布局或复制一套页面。
- 半透明卡片需要真实透出背景，同时保证正文和表单仍然容易阅读。
- 站内搭建日志应同步记录 App 门锁、部署流程和星球玫瑰视觉升级，方便用户、CC 和以后接手的人了解项目历史。

## 修改文件

- `src/themes/prince/assets/rose-paper.jpg`
- `src/themes/prince/backgrounds.css`
- `src/themes/prince/style.css`
- `src/themes/prince/auth.css`
- `src/modules/changelog/ChangelogPage.jsx`
- `docs/02_当前架构.md`
- 本记录

## 数据库、环境变量与部署

- Supabase 表、数据、RLS、账号和环境变量均无变化。
- Toy、`toy_commands` 与 ADB 控制链路均未修改。
- 当前只更新 `codex/star-rose-layout-2-20260727` 预览分支；正式 `main` 尚未修改。

## 实际验证结果

- `pnpm lint` 通过；仅有项目原有警告，没有新增错误。
- `pnpm build` 通过；背景素材被正确打包为 215.41 kB。
- 使用 390×844 手机视图检查开屏与登录页：背景比例正常、没有横向溢出、玻璃卡片可以透出玫瑰。
- 浏览器计算样式确认登录卡片为半透明 `rgba(255, 250, 247, 0.54)`，标题为 `oblique 5deg`。
- 本地预览控制台没有错误。
- 进屋后的首页需要真实账号登录状态，本地未输入或读取主人账号；等待 Cloudflare 分支预览由主人复核。

## 已知风险或未完成事项

- 极宽桌面屏幕使用 `cover` 时会裁切背景上下区域，需要在线预览确认是否符合审美。
- 不支持 `backdrop-filter` 的旧浏览器仍会显示半透明底色，但模糊效果会退化。
- 主 JavaScript 包超过 500 kB 的既有构建提醒仍然存在，本次没有掩盖或顺手重构。

## 下一步计划

1. 推送同一预览分支，等待 Cloudflare 自动构建。
2. 主人登录预览版检查首页、侧栏、搭建日志及正文页的背景可见度。
3. 根据真机反馈微调背景位置或卡片透明度，确认满意后再决定是否合并到 `main`。
