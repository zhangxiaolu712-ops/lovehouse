# 建立共同开发规则与独立变更日志

- 日期：2026-07-26
- 执行者：Codex
- 状态：已完成

## 做了什么

- 新增项目最高优先级开发规则，统一约束 Codex、Claude Code（CC）和以后接手的开发者。
- 建立 `docs/changes/` 专用变更记录目录和标准模板。
- 将数据安全、数据库 migration、模块化、错误处理、验证和 Toy 设备安全列为明确规则。
- 在原有交接文件中加入新规则入口，保留旧历史而不覆盖。

## 为什么这样做

- 原有开发记录集中在超长的 `CLAUDE.md`，继续追加会越来越难查找，也容易产生文档与代码状态不一致。
- 项目将由多个 AI 协作维护，需要一份共同遵守的规则和不会互相覆盖的任务日志。
- 当前项目存在 RLS、身份隔离、数据库版本记录、错误处理和测试不足等风险，需要在继续扩展前建立护栏。

## 修改文件

- `PROJECT_RULES.md`：新增共同开发规则和当前风险优先级。
- `AGENTS.md`：新增 Codex 自动交接入口，指向统一规则和变更日志。
- `docs/changes/README.md`：新增变更记录规范与模板。
- `docs/changes/2026-07-26_建立共同开发规则.md`：记录本次变更。
- `CLAUDE.md`：在顶部增加 CC 的共同规则和独立日志强制入口。
- `docs/03_开发规则.md`：指向新的完整规则。
- `docs/04_搭建日志模板.md`：将独立日志目录设为主要记录方式。
- `docs/05_AI交接说明.md`：加入修改前后必读、必记步骤。

## 数据库、环境变量与部署

- 没有修改数据库。
- 没有修改环境变量。
- 没有执行或修改部署。

## 验证结果

- 已检查新文件路径、标题、必填日志字段和交接入口。
- 本次只有 Markdown 文档变更，不需要执行前端构建。

## 已知风险或未完成事项

- 规则不能自动阻止遗漏，需要 Codex 和 CC 在每次任务中主动遵守。
- Supabase 线上 RLS 与真实表结构仍未核验。
- 还没有通过 CI 自动检查“代码变化时是否同时存在变更记录”。

## 下一步计划

1. 核验 Supabase Auth、RLS 和各表真实结构。
2. 建立 `supabase/migrations/` 并把线上数据库结构纳入版本管理。
3. 统一路由、导航和占位页配置。
4. 建立最小 lint、build 和测试检查流程。
