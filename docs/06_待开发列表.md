# 待开发列表

> 2026-07-18 小婷十六条大纲已归入下表，完成的打勾。

## 高优先级

- [x] 日历天气定位（首页今日卡：日期+定位+实时天气，open-meteo 免费接口）
- [x] AI任务箱与待办事项合并（侧边栏 AI任务箱 → 待办页）
- [x] 碎碎念：原记忆碎片模块改名，按 固定/长期/短期/碎碎念 四级分类浏览与录入
- [x] 私密记录：原动态流改造，默认模糊，点开才能看清
- [x] 搜索整理：全屋关键词检索（记忆/日记/语录/纸条/私密），衔接记忆查找
- [x] 日常记录拆分：日记与碎碎念分开，心情日志独立
- [ ] 短期记忆专区：月经、奶茶等日常小事 + 最近三天日记的聚合视图
- [ ] 长期记忆提炼：①人物事件主题感受简述 ②承诺 ③偏好 ④明确要求记住的事（内容整理，由小克在记忆库执行）
- [x] 统一 Memory System 第一阶段代码地基：固定 GPT/Claude actor、AccessPolicy、单一 MemoryService/Repository、Shared approval 与 Legacy Pending 隔离测试（仅代码，未部署/未建表）
- [x] 统一 Memory System 数据库 migration：Phase 2 V2 schema 已经工程审阅并合并；仍未应用生产，旧 V1 保持退出 active migrations
- [ ] 统一 Memory System Phase 3 Runtime：固定双 MCP、主动保存/召回/修订、Shared 推荐、事务审计/幂等已在独立 Draft 分支实现，等待免费 CI 与工程审阅；未合并、未部署、开关关闭
- [x] House Rules V1：短版规则使用独立版本化数据源，`get_starter_pack` 返回规则 + 原有启动记忆；预留未来 `onSessionStart()` 复用接口，不含 VPS 自动注入
- [x] AI 启动记忆盲盒 V1：固定 actor 随机读取 own private + approved Shared，保留确定 revision/provenance；已合并，未部署
- [ ] AI 日记 V1 + 参与者叙事完整性：继续使用 `save_memory(memory_type=diary)`，服务端固定作者、修订继承、Dream 禁止冒充 AI 新建亲历日记；独立 Draft PR 待审，未部署
- [x] 小客厅 P0 错误透明化：Bridge fence、服务端专用 key 路径与 MCP 错误透明化已合并部署；读取旧消息、唯一消息写入/回读、401 透明化、anon 拒绝和其他 P0 表不可达 smoke 均通过
- [ ] 工单 02 小客厅认证与 Claude OAuth 发现链：发现链及 DCR `authorization_code + refresh_token` 兼容已合并；DCR client registry 跨 Bridge 重启持久化正在独立分支验证，尚未部署；完整生产 Connect 仍属于工单 05 硬验收
- [ ] 工单 03 Claude 一窗口一原生 session：PR #42 已合并、未部署；#113 的 known session 恢复、reset 防陈旧复活与 bounded 一次性 fallback 已在独立 Draft 分支完成，等待复审
- [ ] 工单 04 Claude MCP 最小权限：显式 LoveHouse MCP、13 工具白名单、空内建工具、子进程环境隔离与 MCP init fail-closed 已通过实现层复审；PR #43 保持 Draft、未部署，Claude App 真实 Connect 移入工单 05 作为部署后硬验收
- [ ] Memory Source/Evidence Delta：summary-first、revision source 多对多与 `expand_source` 已进入 Draft PR #44；#123 已将 resolver 与 livingroom 解耦，真实 LoveHouse chat message repository 仍待未来独立工单，生产、Gate B/C 继续锁定
- [ ] Legacy Pending 整理工具：保留原始来源，由小婷、Claude/GPT Curator 或人工逐批决定归属
- [x] 可安装 App + 新设备账号登录 + 日常 PIN 锁屏（已部署并完成真机安装）
- [ ] Supabase RLS 安全策略（现有修复分支待审；完成前禁止应用记忆空间 migration）

## 中优先级

- [x] 星球玫瑰 2.0 布局系统：手机悬浮底栏与快捷房间、桌面宽幅杂志画布、通用档案页外壳
- [ ] AI配置页：显示小克的权限（GitHub/Supabase/网络域名）；身份、记忆加载策略、说话方式归入长期记忆
- [ ] 设置中心 · 备份迁移：一键导出全部数据
- [ ] 纪念日模块（events 表 + 倒计时）
- [ ] 相册模块（gallery）
- [ ] 小游戏区：收藏聊天里一起搓的小游戏（原 App AI模式 取消，改为此）
- [ ] 自动归档：碎碎念/短期记忆过期自动降级

## 未来扩展

- Toy设备
- 手环设备
- 更多AI接入

## 已完成存档

- [x] 小客厅手机端统一主题：真实消息时间戳 + 动态工单簿（状态/优先级/完成划线/跳回原消息）
- [x] 六个基础功能模块 / 五套主题 / GitHub Pages 自动部署
- [x] “星球玫瑰”配套视觉：桌面图标、开屏动画、登录/PIN 与小屋主题
- [x] “星球玫瑰 2.0”新格局：首页信息架构、移动导航、桌面多栏与通用页面档案版式
- [x] 响应式侧边栏 + 六大中心导航
- [x] 记忆系统分级 + 8大类归并 + AI自动读取指令
- [x] 小纸条留言板（notes 表 + 便利贴风格）
- [x] 标签分类抽屉取消（由碎碎念分级 + 搜索整理替代）

## 新模块开发套路

1. `src/modules/xxx/xxxService.js` — 数据层
2. `src/modules/xxx/XxxPage.jsx` — 页面
3. `src/core/router.jsx` — 注册路由
4. `src/shared/AppShell.jsx` — 视情况加导航入口
5. 如需新表 → Supabase 建表，并更新 `docs/02_当前架构.md` 的表结构
6. 按 `docs/04_搭建日志模板.md` 更新 `CLAUDE.md` 变更记录
## 2026-08-09 · Memory System 当前阶段

- [x] Phase 3 Runtime 已完成工程审阅并合并，生产开关仍关闭。
- [ ] Phase 4A Hybrid Recall：embeddings + versioned ranking + keyword fallback 已在独立 Draft 分支施工，等待免费 CI 与 GPT 二审；未合并、未部署。
- [ ] Phase 4B：Anchor + Dream Queue + 可替换 Curator，明确禁止在 Phase 4A 提前实现。
