# 搭建日志

> 当前主要记录方式已经调整为：每次任务在 `docs/changes/` 中新建独立 Markdown 文件。模板和命名规则见 `docs/changes/README.md`。本文件保留用于旧日志兼容和站内搭建日志整理。

日期：

版本：

## 完成内容

-

## 修改内容

-

## 当前状态

-

## 下一步计划

-

---

## 附注 · 使用说明（小克按现状整理）

每次完成修改后，必须先在 `docs/changes/` 建立独立记录。是否同步摘要到 `CLAUDE.md`，根据任务是否影响整体项目状态决定：

- 「完成内容/修改内容」写给下一个接手的 AI 看，具体到文件或模块
- 如果新增了表、字段、环境变量，必须写明
- 「下一步计划」同步到 `docs/06_待开发列表`，完成的打勾 `[x]`
- 站内 📋 搭建日志页面（/changelog）也做同步展示
