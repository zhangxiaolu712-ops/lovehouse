# 04 · 搭建日志模板

每次完成修改后，在 `CLAUDE.md` 的「变更记录」章节追加一条，格式如下：

```markdown
### YYYY-MM-DD | 一句话标题

**操作**: 简述这次做了什么
**分支**: 分支名（如有）

完成内容：
- 具体改动 1
- 具体改动 2
- 验证情况（构建通过 / 截图验证等）
```

## 要点

- 日期用当天日期，标题一句话说清主题
- 「完成内容」写给下一个接手的 AI 看，要具体到文件或模块
- 如果新增了表、字段、环境变量，必须写明
- 同时检查「待开发功能」列表，完成的打勾 `[x]`
