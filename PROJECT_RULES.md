# LoveHouse 施工规则

> 这是 LoveHouse 唯一完整的长期施工规则。`AGENTS.md` 与 `CLAUDE.md` 只做施工入口，不维护第二套规则。

## 快速上手

1. 先确认当前事实，再动手；旧文档、旧聊天、旧 release 不自动代表现在。
2. 一个施工目标一个主分支 / PR；不顺手改无关模块。
3. 能读 Engineering Workspace 时，先读本任务相关的 current revision；不能读时用用户提供的简短接班信息。
4. 普通施工只看相关代码、本轮 diff 和针对性测试；没有风险信号就不全仓库 / 全 VPS 大扫除。
5. 模块保持低耦合，但已知未来会扩展的位置必须留稳定接口，不能把门焊死。
6. 高权限操作可以由 Owner 在前端控制，但 secret、service role 和真正的高权限执行能力必须留在后端。
7. 安全按真实风险分级，不追求银行级复杂度。
8. 用户可见功能没有在真实前端入口看到并可操作，就不算生产完成；纯后端能力按真实后端行为验收。
9. 施工结束要清理临时文件、补丁残骸、无用 worktree / release；未验证就明确写“未验证”。
10. 不再为每次施工创建 `docs/changes`、checkpoint、handoff 或日期流水 Markdown。

## 1. 当前事实与开工方式

当前事实优先级：

1. 当前任务明确要求。
2. 当前相关代码、Git 分支 / diff、真实运行状态、数据库实测。
3. Engineering Workspace 中与本任务相关的 current revision 与已验证事实。
4. `PROJECT_RULES.md` 与 `docs/02_当前架构.md`。
5. 旧文档、旧 worktree、旧 release、旧聊天只在追溯历史原因时按需查看。

禁止为了“放心”在每个小任务开始时重扫全仓库、全 VPS、全部 worktree。普通施工只读取本任务相关代码和一层直接依赖；出现风险信号后再局部扩大。

如果同一句需求有多个合理解释，而且不同解释会影响数据库、删除、部署、域名、OAuth、权限、VPS 配置、架构或 Git 合并，先问用户，不猜。

## 2. 施工范围、分支与 PR

- 一个施工目标使用一个主分支 / PR。
- 为完成同一目标必须补的小洞可以留在同一分支。
- 独立功能、无关问题、明显扩大范围的问题，尤其数据库 / 权限 / 部署高风险事项，应停下来说明并另开任务或征求用户决定。
- 不直接在 `main` 上施工。
- 规则文件修改必须单独 PR，不与功能、前端、Bridge、数据库施工混在一起。
- 不为了“代码更漂亮”顺手大重构。

## 3. 模块化、低耦合与扩展口

LoveHouse 要低耦合，但低耦合不等于把模块封死。

- 已知未来会增加的能力，应提前留稳定扩展口；新增能力优先“接进去”，不要每次拆墙重造。
- 页面主要负责界面和交互，数据访问与业务动作尽量留在各模块自己的 service / domain 层。
- 同一种逻辑被两个以上模块分别复制、改出不同版本时，考虑抽成公共层。
- 一个小功能如果需要同时修改 4 个以上互不相关的位置，先检查是不是职责或接口设计出了问题；这只是报警线，不是机械重构命令。
- 跨模块共享可变状态、全局有状态单例、模块 A 改状态导致模块 B 隐式变化，都是高耦合信号。
- 禁止循环依赖。

### 可以共享什么

可以共享：类型、常量、schema、纯函数、无状态工具、稳定契约。

不要为了省几行代码共享：跨模块可变状态、有状态单例、万能数据访问 service。

共享 schema 不等于共享数据入口；各模块仍通过自己的 service / domain 维护数据职责。

## 4. 前端、Bridge、Service 与 Supabase

不强制所有普通页面都绕 Bridge。

- 普通低风险业务数据，在 Auth + RLS 边界清楚时，可以通过模块 service 使用 Supabase 客户端。
- Memory、Engineering、Provider、跨服务编排、secret / service-role、系统级权限等高权限能力必须由后端承载。
- 高权限能力可以在前端提供 Owner 开关、按钮和确认入口，让用户自己控制；真正凭证和执行仍留在 Bridge / 服务端。
- service role、secret、token 不得进入前端 bundle、仓库文档、日志或 Engineering 正文。
- Provider adapter 保持薄，只负责适配 provider / runtime 契约，不反向接管前端状态。

## 5. 原生 App 原则

原生 LoveHouse 采用“空壳先行、后端复用、逐模块点亮”。

第一阶段先做可安装、可打开、可导航、可登录、能连接现有后端的原生空壳。它本质上是 LoveHouse 前端 + Android 原生能力外壳。

相机、照片、文件、麦克风、音频、通知、定位、分享、生物识别、本地缓存等已知未来能力要预留入口和状态，但不要求第一版全部实现。

不因为做 Android App 重建第二套后端。现有 Bridge / API / Supabase / Provider 能力优先复用。

## 6. Engineering Workspace 与窗口连续性

Engineering Workspace 是 LoveHouse 的长期工程记忆区，用于保存：当前主线、重要施工结论、已验证事实、架构边界、错误经验、后续待办和来源证据。

它不是生活记忆，也不是仓库里的 Markdown 流水账。

- 能访问 Engineering 时，新施工窗口优先读取本任务相关的 current revision，再看相关代码。
- 容易被遗忘的小功能 / 小需求也可以进入 Engineering 的稳定待办或功能链，不依赖用户一直记着。
- 无法访问 Engineering 时，使用用户提供的简短接班信息继续。
- 两种情况都不允许因为“接班”重新全盘考古。
- 同一长期工程事实使用稳定 subject / revision 维护，不因每次变化新建一份 Markdown。

## 7. 文档边界

仓库长期只保留真正需要随代码存在的资料：

- `PROJECT_RULES.md`：唯一完整施工规则。
- `AGENTS.md` / `CLAUDE.md`：很薄的施工入口。
- `docs/02_当前架构.md`：薄的当前架构、住户表和稳定配置。
- 与现役代码直接绑定、无法仅靠代码 / 测试表达的必要 contract / schema 说明。

禁止重新建立：

- 每任务 `docs/changes`。
- `checkpoint-final-final.md` 一类进度文件。
- 每窗口 handoff Markdown。
- PID、当前 SHA、今天部署到哪之类易变状态快照。
- 第二套、第三套开发规则。

历史由 Git 和 Engineering Workspace 承担。

新增非代码文件前先判断它是否必须长期跟仓库存在；拿不准就问用户，不默认留下。

## 8. 安全分级：够用，不做银行级

LoveHouse 是个人自用项目。安全重点保护账号、私密数据、Owner 权限、secret / token、高权限数据库写入和现实设备控制；普通低风险功能不为了理论完美叠加多层审批和代理。

### A 级：必须先问用户

- `DROP`、`TRUNCATE`、大批量删除 / 覆盖、不可逆数据迁移。
- Auth / RLS / Owner 权限边界变化。
- OAuth、主域名、关键生产配置变化。
- secret / token / service-role 处理或轮换。
- 删除仍可能承担当前生产、必要回滚、唯一证据或独有代码的资产。
- 高风险现实设备控制策略变化。

### B 级：告知即可执行

- 已有流程内的正常生产部署。
- 普通 PM2 / Bridge / sidecar restart。
- Git merge。
- 已确认无用的 worktree / 历史 release 清理。
- 已有 rollback 的常规 migration。

### C 级：正常施工自行执行

- `git status` / `git diff` / 代码搜索。
- 只读 runtime / 数据库检查。
- 针对性测试、lint、build。
- 普通本地 smoke 与静态检查。

## 9. 数据库与不可逆操作

- schema 变化使用 migration / 可复现 SQL；不能只在控制台偷偷改。
- 风险 migration 需要合理 rollback / 恢复路径。
- 不批量删除旧 Memory、revision、source、audit 或其他长期数据，除非用户明确批准并已完成迁移 / read-back。
- Auth + RLS 是前端直连 Supabase 的实际安全边界；不能用“前端看不到 key”冒充安全。
- 数据库和接口状态必须以真实 read-back / 权限测试判断，不能只看 migration 成功消息。

## 10. 补丁叠补丁与结构信号

第一次 workaround 可以用于救急，但要知道它是 workaround。

同一位置第二次又要补 workaround 时，先看根因；出现第三层、多个 workaround 相互依赖、同一文件近期因不同理由反复补、兼容层明显膨胀时，默认视为结构风险，先局部检查，不继续盲叠。

默认只利用本轮 diff 和相关链路检查，不因此全仓库大审查。

## 11. 临时文件、生成文件与旧资产

临时文件 = 只服务本次施工，任务结束后删除也不影响当前功能、回滚、长期规则、稳定 contract 或唯一证据。

施工结束应清理临时截图、一次性导出、调试产物、checkpoint、临时 handoff、补丁说明等。

生成文件按长期职责判断：

- build 产物、缓存、一次性报告不进仓库。
- 需要随代码长期测试 / 复现的 fixture、模板、生成器正式输出可以进仓库。

`legacy`、archive、旧 worktree、历史 release、rollback 不是永久收藏品。确认不再承担现役、回滚、唯一证据、独有代码、未迁数据或明确复用价值后，应清理掉。

## 12. 验证、生产状态与“厨房归位”

施工状态必须分清：代码完成、测试通过、已合并、已部署、后端运行正常、生产完成，不得混写。

### 用户可见功能

如果功能本来应该由用户在前端看到或操作，那么只有真实生产前端入口已经可见、可操作，才叫“生产完成”。API 200、PM2 online、CI 绿色或已 deploy 都不能单独等同生产完成。

### 纯后端能力

内部任务、migration、embedding、MCP 路由、RLS、audit、内部 health 等没有前端产品入口的能力，按真实后端行为、read-back、权限测试、调用结果等完成验收，不为了凑“前端可见”硬做页面。

### 默认收尾

普通施工结束只做：

- `git status` / `git diff`。
- 本轮改动文件及一层直接调用方 / 依赖检查。
- 与风险匹配的针对性测试。
- 清理临时文件、无用产物、半成品和明显 patch 残骸。

只有看到风险信号才局部升级检查。

谁施工，谁归位。功能做出来但厨房还乱着，不算这一轮真正收尾。

未实际执行的验证必须写“未验证”及原因，禁止冒充通过。

## 13. 聊天 CLI 与施工 CLI 隔离

施工 Codex / Claude 与自建前端中的聊天 Codex / Claude 必须使用独立工作目录 / 指令边界。

- 工程根目录 `AGENTS.md` / `CLAUDE.md` 只服务施工。
- 聊天 CLI 使用自己的独立 cwd 和聊天侧提示词文件。
- 不让 Git、PR、数据库施工、安全审批等工程指令污染普通聊天。
- 也不把聊天人格 / 风格提示塞回工程根目录。

## 14. 完成标准

一轮施工完成至少意味着：

- 用户要求的范围真正完成，没有偷偷扩范围。
- 当前 diff 清楚，没有无关文件和临时垃圾。
- 没有明显重复实现、补丁裂缝和半完成代码被塞进 main。
- 针对性验证通过，或明确记录未验证原因。
- 用户可见功能满足真实前端验收；纯后端功能满足自身真实后端验收。
- 需要长期保留的工程事实进入 Engineering，而不是新建流水 Markdown。
- 最后给用户一份简短、好读的小手帐：做了什么、验证了什么、还有什么没做。
