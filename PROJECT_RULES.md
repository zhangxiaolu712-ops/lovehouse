# LoveHouse 共同开发规则

> 本文件是 LoveHouse 当前最高优先级的开发约定，适用于 Codex、Claude Code（CC）以及以后接手项目的任何开发者或 AI。
>
> 原有 `CLAUDE.md` 保留身份、项目历史和旧交接信息，不删除；当旧说明与本文件冲突时，以本文件为准。

## 一、项目目标

LoveHouse 是一个长期成长的个人数字空间。开发目标不是快速堆功能，而是保证：

- 用户数据安全、可导出、可迁移。
- 每个功能可以独立维护和替换。
- 不推倒重建，不复制出第二套项目。
- 每次改动都有原因、验证结果和下一步计划。
- 换一个 AI 或开发者后，仍然能继续维护。

## 二、修改前必须执行

任何人或 AI 修改文件前，必须：

1. 阅读本文件。
2. 阅读 `CLAUDE.md` 中与当前任务有关的项目状态。
3. 查看 `docs/changes/` 中最近的变更记录，避免重复开发或覆盖未完成工作。
4. 查看当前实际代码，不只依赖文档判断功能状态。
5. 明确本次任务范围；不顺手修改无关模块。
6. 涉及数据库、权限、部署或删除数据时，先说明影响，再执行。

如果用户只要求分析或检查，不得修改项目文件。

## 三、修改后必须执行

只要本次任务修改了项目文件，就必须在结束前：

1. 在 `docs/changes/` 新建一份变更记录，一次任务一个文件。
2. 写清楚：做了什么、为什么、修改了哪些文件、如何验证、当前风险、下一步计划。
3. 如果数据库表、字段、RLS、环境变量或部署流程发生变化，必须单独注明。
4. 如果完成或新增了计划项，同步更新 `docs/06_待开发列表.md`。
5. 如果页面结构或表结构变化，同步更新 `docs/02_当前架构.md`。
6. 执行与改动风险相匹配的检查；不能执行时，记录原因，禁止写成“已验证”。

变更记录命名：

```text
docs/changes/YYYY-MM-DD_简短任务名.md
```

同一天重名时增加序号：

```text
2026-07-26_日记编辑功能_02.md
```

## 四、代码结构规则

### 1. 新功能必须模块化

普通业务模块至少包含：

```text
src/modules/功能名/
├── XxxPage.jsx
└── xxxService.js
```

- Page 负责界面、交互和状态展示。
- Service 负责 Supabase 查询和数据变更。
- 页面内原则上不直接写 Supabase 查询。
- 跨模块共用逻辑放入 `src/shared/` 或明确的公共层，不复制粘贴。

### 2. 不制造重复入口

- 路由统一在 `src/core/router.jsx` 注册。
- 导航结构统一由公共配置管理；新增功能不能在多个文件分别复制一份页面信息。
- 旧版代码位于 `legacy/`，只作存档，不继续维护，不把新功能写进去。

### 3. 每个页面必须处理完整状态

读取数据至少要处理：

- 加载中
- 成功
- 空数据
- 失败

写入数据至少要处理：

- 提交中，防止重复点击
- 成功反馈
- 失败反馈
- 删除确认或可恢复方案（视数据重要程度）

### 4. 避免硬编码继续扩散

关系日期、人名、默认城市、空间配置等需要集中管理；新增代码不得在多个页面重复写相同常量。

### 5. 不做无关重构

- 修一个功能时，不顺便重写其他模块。
- 大功能拆成小任务、小提交。
- 不删除现有功能，除非用户明确要求并已确认影响。
- 不为了“代码更漂亮”进行没有实际收益的大规模改写。

## 五、数据库与隐私规则

以下内容属于最高风险，必须优先于新增普通页面：

1. Supabase Auth 与用户身份。
2. 所有业务表的 RLS。
3. 数据归属字段，例如 `user_id` 或 `household_id`。
4. 数据库 migration 和可恢复方案。
5. 日记、记忆、私密记录和设备指令的访问控制。

强制规则：

- Supabase anon key 可以用于前端，但不能依靠“隐藏 anon key”保护数据，安全必须由 Auth + RLS 保证。
- 禁止把 `service_role` 密钥写入前端、仓库或变更日志。
- 数据库结构变化必须留下 migration/SQL 记录，不能只在控制台手工修改而不留痕。
- 未经用户明确确认，不执行批量删除、覆盖或不可逆的数据迁移。
- “界面模糊”不等于私密保护；私密数据必须在数据库权限层隔离。
- 日志中不得复制真实密钥、令牌、完整私密内容或不必要的个人数据。

## 六、Toy 设备安全规则

Toy 会影响现实设备，风险高于普通页面。任何相关修改必须考虑：

- 命令发送者身份和设备绑定。
- 强度范围、频率限制和命令过期时间。
- 紧急停止必须始终可用且优先。
- 页面退出、断网和脚本异常时的停止策略。
- 未真正执行的命令不得错误标记为已执行。
- 自动模式必须清理计时器，避免后台继续下发。

Toy 安全没有完成前，不应扩大公开访问范围。

## 七、验证要求

根据改动选择检查：

- `npm run lint`
- `npm run build`
- 相关自动测试
- 桌面端与移动端页面检查
- 四套主题检查（涉及共用样式时）
- Supabase RLS 正向和反向权限测试（涉及数据权限时）
- GitHub Actions 部署状态（涉及发布时）

变更记录必须如实写明实际执行了什么。环境中缺少命令、网络或权限时，写“未验证”及原因。

## 八、当前风险优先级

### P0：继续公开使用前优先确认

- 核验并启用 Supabase RLS。
- 建立真实身份和数据归属。
- 限制 `toy_commands` 的写入、读取和执行。
- 建立数据库 migration。

### P1：新增大量功能前处理

- 统一路由、导航和占位页配置。
- 统一数据访问层和错误处理。
- 增加最小自动测试与 CI 检查。
- 完成备份导出方案。

### P2：持续整理

- 减少硬编码和重复常量。
- 修正文档与实际代码不一致。
- 为现有内容模块补编辑、分页和失败反馈。
- 明确 `legacy/` 仅为只读存档。

## 九、完成标准

一个任务只有同时满足以下条件才算完成：

- 用户要求的功能或修复已实际完成。
- 没有无关文件被误改。
- 已执行必要验证，或明确记录无法验证的原因。
- 相关架构/计划文档已同步。
- `docs/changes/` 中已有本次变更记录。

## 十、GitHub 审阅与部署闭环

每一轮做到可验收状态后，必须先关闭本轮，不因下一条新需求而跳过交付：

1. 整理变更记录并完成必要检查。
2. 提交到独立 Git 分支并推送 GitHub。
3. 创建 Draft PR，写清范围、风险、验证结果、回滚方式与待人工确认点。
4. 由小婷在 GitHub 查看变更；确认后再把 PR 标记为可审阅并合并。
5. **合并不等于部署。** 是否部署、部署到哪个环境，必须单独确认和记录。
6. 部署后执行线上冒烟检查，并在变更记录或小客厅写明：已合并、已部署、线上验证结果。

为避免注意力转移导致漏项：上一轮未标明“已提交 / 已合并 / 已部署”三种状态前，收到新需求时也要先报告上一轮所处阶段。UI、文档等低风险改动不得与数据库迁移、RLS、OAuth、Bridge、VPS 或 Toy 等高风险改动混在同一个 PR。
- 已向用户说明结果、风险和仍未完成的事项。

## 十一、工程资料归属与清理边界

- `docs/00_工程边界与资料索引.md` 是工程资料长期入口；接班时必须和本文件一起读取。
- Engineering Workspace 保存详细工程事实、历史 revision、source evidence、部署/故障经验；GitHub / VPS 文档只保留当前规则、架构、规划、索引和与代码变更对应的 change record。
- 旧 checkout、worktree、release snapshot 与临时 handoff 只作为迁移来源或证据，不自动视为当前事实。
- 同一工程事实按稳定 `subject_key` 维护 revision history，不采用“一份旧文件 = 一条工程记忆”的导入方式。
- 清理旧资料前必须先完成 Engineering 迁移、读回验收和 unique orphan 核对；当前生产 release、必要 rollback 和尚未迁移的独有成果不得删除。
- 工程资料与生活记忆继续严格分流，纯工程事实不得写入 GPT / Claude 的生活 private Memory。
